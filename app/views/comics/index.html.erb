<h1>All Comics</h1>

<%= form_tag(comics_path, method: :get) do %>
    <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag ("Search") %>
    <% end %>
<% @comics. each do |comic| %>

    <div>
    <%= link_to(comic.title_and_category, comic_path(comic) )%>
    </div>

    <% end %>